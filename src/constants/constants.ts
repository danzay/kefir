/** The module keeps constants. */

import { elementSizeType } from "../models";

export const DATE_TEMPLATES = {
    COMMENTS: "dd.MM.yyyy, HH:mm:ss"
};

export const  SIZE_VALUES: Record<elementSizeType, number> = {
    s: 22,
    m: 30,
    l: 38
}

